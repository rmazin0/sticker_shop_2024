import {useState} from 'react'
import Nav from '../components/Nav';

const Hero = (props) => {
    return (
        <div>
            <Nav/>
            Hero page
        </div>
)}

export default Hero;